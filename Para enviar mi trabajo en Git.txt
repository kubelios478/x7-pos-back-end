Para enviar mi trabajo en Git

En el GitHub: SyncFork en el Forked que cree

En el Git Bash: 

git checkout main
git pull origin main---> Para traer todo lo del Main


git checkout subcripcion
git merge main para iniciar el Merge de lo que hice con los demás

Luego ve al editor de código para aceptar y corregir los cambios

Después de eso hago un git commit en el editor

Luego vuelvo al Bash para: git push origin subcripcion--> Para Traer todo lo que hice al Main

Después voy al GitHub: Cambio del Main al Subcripcion para aplicar SyncFork y luego Contribute para hacer el PULL REQUEST

Si me aceptan lo que hice, repite

Si no lo aceptan aplica los cambios y aplica desde el push
